export function calculateAverage(){
    let one = Number (prompt("Введите первое число"))
    let two = Number (prompt("Введите второе число"))
    let tree = Number (prompt("Введите третье число"))
    alert((one + two + tree) / tree)
}
calculateAverage()